.env*
!.env.example
.temp/
.DS_Store

# Binaries for programs and plugins
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out

# Output of the go coverage tool, specifically when used with LiteIDE
*.coverprofile

# Output of the go build tool
_build/
bin/

# Go workspace file
go.work

# GoLand project files
.idea/

# GoLand workspace file
*.iml

# Dependency directories (remove the comment below if you use Go modules)
# /vendor/

docker/.env

node_modules

.serena/

# Claude Code: ignore personal settings + auth, keep team-shared settings.json/agents/commands
.claude/settings.local.json
.claude/.credentials.json
.claude/

# understand-anything generated knowledge graph (regenerable; per-commit snapshot)
.understand-anything/.claude/
.claude/worktrees

# limen CLI schema export (regenerated on boot when LIMEN_SECRET is set)
.limen/
