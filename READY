hexagonal-domain-split refactor complete on main

Phase 0 (scaffolding + rules + input sweep):
  f150469 plan
  4b2aeb0 hexagonal & DDD rules doc
  03c6cf4 row_helpers.go scaffold
  13990f9 + e47874b + e2f83c7 32 *Input types from domain/ to port/
  c7e8f08 + 1f2ddf8 + b5ea994 merges that landed Phase 0 on main

Phase 1 (20 entity splits, one commit per group):
  aebaa16  User
  3b367e0  Session
  d1e65e5  Setting
  cf92cc2  MetadataStore
  27afe5d  Org
  f127d4a  PspConfig
  1488f1d  Refund
  2c6e795  ApiKey (transient RawKey lifted to port.CreatedApiKey)
  2fe170b  WebhookSubscription
  c268413  Customer (+ FindByIds)
  3257b3e  Price (+ FindByIds + FindByVariantIds)
  f53db84  PaymentMethod
  b804e6a  OrderItem + Variant + Subscription + Order + Product (Wave 3+4)
  e36a948  Payment
  a88ec99  Cart
  57cb289  Dunning bundle (6 entities)

Phase 3 (verification): build clean, unit + integration tests pass,
go vet clean. internal/core/domain/ has zero gorm tags, zero TableName
methods, zero gorm.io imports. Persisted entity types have zero json
tags (value objects retain them as their JSON-column / wire shape per
docs/internal/hexagonal-mapping-pattern.md).

Counts:
  - 20 *_row.go files in internal/adapter/postgres/
  - 27 row types + 26 toDomain mappers
  - 32 *Input types in internal/core/port/
  - Subscription's embedded Customer + OrderItem stay on the domain as
    plain Go fields (no tags); same for Order's Customer + Items,
    OrderItem's Price, Product's Variants, Variant's Prices. These are
    populated by repo Preload at the row layer. The pure-aggregate
    refactor (cross-aggregate-by-ID-only + read models) is a follow-up.

Known follow-ups (not blocking):
  - Promote inbound port interfaces (DunningService, OrderWorkflowService,
    etc.) out of port/ if the project later prefers split inbound/outbound
    packages — for now, port/ holds ALL ports per the user's stated
    layout.
  - Aggregate-boundary purification per the rules doc (drop embedded
    related-entity fields from domain; add Read Models per Q3 Answer 2
    when a composed query has a real use-case name).
  - Per-entity DTO file split for response.go is partially done (per-entity
    request/response files added for Session and Org; the legacy
    response.go still contains the older entity response DTOs).
